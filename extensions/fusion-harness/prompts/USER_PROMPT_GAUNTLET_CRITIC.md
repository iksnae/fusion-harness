Audit the current state of the project at {{CWD}} against the rubric below. Round {{ROUND}} of {{MAX_ROUNDS}}.

Open the real files. Every criterion names the evidence that settles it — go and look at that evidence yourself.

Return ONE raw JSON object per your output contract. Nothing else.

# ORIGINAL REQUEST (what was asked for)
{{PROMPT}}

# RUBRIC (the entire bar — {{CRITERIA_COUNT}} criteria)
{{RUBRIC}}
